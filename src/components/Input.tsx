import { ark } from '@ark-ui/react'

import { panda } from '@/styled-system/jsx'
import { input } from '@/styled-system/recipes'

export const Input = panda(ark.input, input)
export type InputProps = typeof Input
